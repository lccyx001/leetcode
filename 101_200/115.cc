#include "common.hh"
using namespace std;
int numDistinct(string s, string t) {
  int n = s.size(), m = t.size();
  if (n < m) return 0;
  int cnt = 0, i = 0;
  while (i < n) {
    for (int j = 0; j < m; j++) {
        
    }
  }
}